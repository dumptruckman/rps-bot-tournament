# Domain Docs

This is a single-context repository.

Before exploring or changing the codebase:

- Read `CONTEXT.md`.
- Read the relevant accepted decisions under `docs/adr/`.
- Use the glossary's exact domain vocabulary.
- Surface any conflict with an existing ADR rather than silently overriding it.
